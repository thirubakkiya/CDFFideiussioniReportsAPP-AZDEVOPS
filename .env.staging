# Staging (Pre-Production) Environment Configuration
# Set NODE_ENV to 'staging' to use staging configuration

NODE_ENV=staging
REACT_APP_ENV=staging

# Optional: Override Staging API URL
# REACT_APP_API_URL_STAGING=https://staging-api.sella.it/cdf-reports/api

# Debug logging enabled for staging
REACT_APP_DEBUG=true
